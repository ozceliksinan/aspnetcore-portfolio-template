@model IEnumerable<Hobilerim>

<section class="resume-section" id="interests">
    <div class="resume-section-content">
        <h2 class="mb-5">Hobilerim</h2>
        @foreach (var item in Model)
        {
            <p>@item.Aciklama1</p>
            <p class="mb-0">@item.Aciklama2</p>
        }
    </div>
</section>